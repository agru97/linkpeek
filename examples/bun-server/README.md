# Bun — Link Preview Server

A minimal Bun server that returns link preview metadata.

## Setup

```bash
bun add linkpeek
bun run index.ts
```

## Usage

```bash
curl "http://localhost:3000?url=https://github.com"
```
